@model IEnumerable<ShoeEcommerce.Model.Products.Brand>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Share/LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameBrand)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameBrand)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdBrand">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdBrand">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdBrand">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
